query getGravCmsPage($urlKey: String!) {
    gravCmsPage(url_key: $urlKey) {
        url_key
        content
        content_heading
        title
        page_layout
        meta_title
        meta_keywords
        meta_description
    }
}
