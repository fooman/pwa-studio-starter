mutation sendSupportMessage(
    $name: String!
    $message: String!
    $email: String!
){
  sendSupportMessage (
    input:
    {
      name: $name,
      message: $message,
      email: $email
    }
  ) {
    status_message
  }
}
